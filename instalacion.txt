pip uninstall -r requirements.txt -y
cd backend
py -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
uvicorn app:app --reload --port 8000

# En la carpeta backend con el entorno virtual activado
python

>>> from database import engine, Base
>>> from models import *
>>> Base.metadata.create_all(bind=engine)
>>> exit()

ngrok http 8000

cd frontend-react
npm install @mui/icons-material
npm run start

URLs importantes:

Docs: https://hgw-chatbot-whatsapp-business.onrender.com/api/docs
Health: https://hgw-chatbot-whatsapp-business.onrender.com/api/health
Webhook: https://hgw-chatbot-whatsapp-business.onrender.com/webhook


Automáticamente con UptimeRobot (RECOMENDADO):
Para que NUNCA se duerma:
https://dashboard.uptimerobot.com/monitors

Si está dormido, haz click en el botón "Manual Deploy" → "Clear build cache & deploy"